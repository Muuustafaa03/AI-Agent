# --- App ---
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost:8080

# --- OpenAI ---
OPENAI_API_KEY=sk-REPLACE_ME

# --- Database ---
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=agentdb
DB_USERNAME=agentuser
DB_PASSWORD=agentpass

# --- SMTP (optional) ---
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=replace
SMTP_PASS=replace
SMTP_FROM="Agent Bot <bot@example.com>"

# --- Python Worker ---
PY_WORKER_URL=http://python:5001